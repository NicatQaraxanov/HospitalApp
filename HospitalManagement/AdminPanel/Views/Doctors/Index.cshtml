@model IEnumerable<Doctor>
@{
    ViewData["Title"] = "Index";
}


<div class="container p-3">
    <div class="row pt-4">
        <div class="col-6">
            <h2 class="text-primary">Doctors</h2>
		</div>
        <div class="col-6 text-end">
            <a asp-controller="Doctors" asp-action="Create" class="btn btn-primary">
               <i class="bi bi-plus-circle"></i> &nbsp; Add Doctor
			</a>
		</div>
	</div>
    <br /><br />

    <table class="table table-bordered table-striped" style="width:100%">
        <thead>
            <tr>
                <th>
                    Name
			    </th>
                <th>
                    Surname
			    </th>
                <th>
                    Age
			    </th>
                <th>
                    Position
			    </th>
                <th>
                    Experience
			    </th>
                <th></th>
		    </tr>
	    </thead>
        <tbody>
            @foreach(var obj in Model)
            {
                <tr>
                    <td>
                        @obj.Name
				    </td>
                    <td>
                        @obj.Surname
				    </td>
                    <td>
                        @obj.Age
				    </td>
                    <td>
                        @obj.Position
				    </td>
                    <td>
                        @obj.Experience Years
				    </td>
                    <td class="text-center">
                        <div class="w-75 btn-group" role="group">
                            <a asp-controller="Doctors" asp-action="Edit" asp-route-id="@obj.Id"
                            class="btn btn-primary mx-2"> <i class="bi bi-pencil-square"></i> Edit</a>
                            <a asp-controller="Doctors" asp-action="Delete" asp-route-id="@obj.Id"
                            class="btn btn-danger mx-2"> <i class="bi bi-trash-fill"></i> Delete</a>
                            <a asp-controller="Doctors" asp-action="Patients" asp-route-id="@obj.Id"
                            class="btn btn-info mx-2"> <i class="bi bi-person"></i> Patients</a>
					    </div>
				    </td>
			    </tr>
            }
        </tbody>

    </table>
</div>